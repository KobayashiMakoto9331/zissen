<% @title= '職員アカウントの編集'%>
<h1><%=@title%></h1>

<div id="genetic-form">
  <%= form_with model: @staff_member, url: admin_staff_member_path, local: true do |f| %>
    <%= render "form", f: f %>
    <div class = "buttons">
      <%= f.submit "登録" %>
      <%= link_to "キャンセル", admin_staff_members_path %>
    </div>
  <%end%>
</div>