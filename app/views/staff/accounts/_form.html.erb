<%= markup do |m|
p = StaffMemberFormPresenter.new(f, self) 
m << p.notes 
  p.with_options(size: 32, required: true) do |q|
    m << q.text_field_block(:email, "メールアドレス") 
    m << q.full_name_block(:family_name, :given_name, "氏名") 
    m << q.full_name_block(:family_name_kana, :given_name_kana, "氏名") 
  end
end%>
