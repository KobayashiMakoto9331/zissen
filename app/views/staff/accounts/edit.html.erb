<% @title = "アカウント編集" %>
<h1><%= @title %></h1>

<div id="genetic-form">
  <%= form_with model: @staff_member, url: staff_account_path do |f| %>
  <%= render "form", f: f %>
    <div class="buttons">
    <%= f.submit "更新" %>
    <%= link_to "キャンセル", staff_account_path %>
    </div>
  <%end%>
</div>