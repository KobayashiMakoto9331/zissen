<% @title = "アカウント編集" %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_with model: @staff_member, url: confirm_staff_account_path do |f| %>
  <%= render "form", f: f, confirming: false %>
    <div class="buttons">
    <%= f.submit "確認画面に進む" %>
    <%= link_to "キャンセル", staff_account_path %>
    </div>
  <%end%>
</div>