<header>
  <span class = "logo-mark">BAUKIS2</span>
  <%= link_to "トップに戻る", staff_root_path, class: "logo-mark" %>
  <%= content_tag(:span, flash[:notice], class: "notice") if flash.notice %>
  <%= content_tag(:span, flash[:alert], class: "alert") if flash.alert %>
  
  <%=
    if current_staff_member
      link_to "ログアウト", staff_session_path, method: :delete 
    else
      link_to "ログイン", staff_login_path
    end
  %>

  <%= link_to "アカウント", staff_account_path if current_staff_member %>
  <%= number_of_unprocessed_messages if current_staff_member %>
</header>